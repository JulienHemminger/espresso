import numpy as np

def get_elc_forces(p3m, gap_size, pw_error, system):
    # do i use dipol-specific simplifications (F2 = -F1)?


    """
  
    
    """
    return [np.array([0, 0, 0]), np.array([0, 0, 0])]